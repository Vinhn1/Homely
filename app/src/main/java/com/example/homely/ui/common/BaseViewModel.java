package com.example.homely.ui.common;

import androidx.lifecycle.*;

/**
 * ViewModel cơ sở (có thể thêm xử lý chung như CompositeDisposable).
 */

public class BaseViewModel extends ViewModel {
    // TODO: add common logic if needed
}
